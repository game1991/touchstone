# 交接文档：touchstone — 项目结论生命周期管理机制

> Internal implementation history from the original design session; kept for provenance. Start with README.

> 日期：2026-09-11
> 状态：设计方案 v1.0 定稿（含多维矩阵评审修订），**待最终优化与试点落地**
> 给接手会话：本文件是唯一入口，读完即可开工。

---

## 1. 一句话背景

用户要求 AI 助手学会一套「项目结论生命周期管理（touchstone）」技巧：项目进度/阶段性成果/结论性认识记录进按时间排序的项目管理文档，**只有已验证且用户同意归档的结论才能作为可信事实**；对话中未验证的结论标记为"假设中"与已归档结论严格区分，避免过时结论导致 AI 助手做无用功。

**触发案例**（理解动机必读）：2026-09-11，某监控平台项目。9/10 会话记录的「某平台只转发自有会话 cookie」是当时验证过的结论，9/11 线上实测推翻（平台会全量转发浏览器 cookie，过期会话 cookie 抢占身份链导致 401 故障）。暴露的问题：记忆无置信度分级、无淘汰机制，过时结论与有效结论被同等引用。

## 2. 已完成的工作

1. **需求头脑风暴**：与用户多轮收敛出核心设计（载体/状态机/门禁/分工/落地路径），用户认可「四态状态机 + 用户归档门禁」两个关键点。
2. **多维矩阵评审**：用户要求对标主流实践（Harness/框架模式）评审。已执行五维评审（正确性/架构契合/可执行性/安全治理/可演进性），检索对标三类主流实践并全部通过：
   - **ADR（Nygard/MADR）**：状态机对标其 Proposed/Accepted/Superseded/Deprecated 生命周期；条目不可变+双向链接（`Superseded by xxx`）模式已采纳。
   - **LLM 记忆生命周期最佳实践**（2026 生产经验）：写入管线人工审批、READ/WRITE/UPDATE/FORGET/VERIFY 五操作、TTL/时间衰减、检索 Top-K 而非全量注入——已吸收为 §3 防膨胀规则。
   - **CLAUDE.md/AGENTS.md 记忆文件分层共识**：进仓库随代码走、索引+详情分层（<200 行索引）、像代码一样版本化管理记忆文件。
3. **评审修订项已全部合入 DESIGN.md**：P0（索引区检索、UPDATE/FORGET 语义）、P1（强制 TTL、语义去重）、P2（长度红线、批准人溯源）。
4. **产出物**：本仓库 `DESIGN.md`（设计方案 v1.0 定稿）。

## 3. 关键设计决策（已与用户对齐，不要推翻）

- **用户归档门禁不可妥协**：只有用户明示归档才转"已归档"，AI 助手不得自我归档。这与用户另一条已确认的偏好「鉴权/安全边界决策必须与用户确认」同源。
- **推翻/废弃不删除原文**：保留被推翻结论+推翻证据（双向链接），演进链路本身是核心价值。
- **每仓库一份 TOUCHSTONE，不建全局大文档**。
- **五态锁死**（假设中/验证中/已归档/已推翻/已废弃），禁止状态蔓延。
- **两阶段落地**：先记忆规则轻量跑两周，再评估 skill 化（`/archive`）。

## 4. 待办（按优先级，供新会话接手）

1. **最终优化**：通读 DESIGN.md，检查五防膨胀规则的可执行细节（如索引行格式、复审降级的具体判定语句），按新会话的判断微调。
2. **用户确认剩余权衡项**：复审期限值（当前建议 3 个月）、是否 skill 化（当前建议两周后再定）——这两项在头脑风暴中用户尚未拍板。
3. **试点落地**：在首个试点仓库创建 TOUCHSTONE.md（含索引区骨架），把存量记忆中已验证的项目结论迁移为首批"已归档"条目，交用户逐条确认归档。
4. **记忆规则写入**：三条核心规则写入助手的项目记忆（feedback 类），使机制在所有会话自动生效。
5. **两周复盘**：检查起草/归档频次与"假设中被当事实"事故率，决定是否 skill 化。

## 5. 风险与注意

- **机制腐化风险**：最大的失败模式是"起草多、归档少"，候选区堆积后与旧记忆一样不可信。若试点两周候选区超过 10 条未裁决，主动提醒用户批量裁决。
- **不要过度工程化**：用户风格偏好轻量务实（参考 轻量务实的黑盒交付模式：方案陈述用影响维度，不抛技术选择题，但**安全/治理类决策必须确认**）。字段越少越可持续，勿在后续优化中加字段。
- **评审资料**：如需追源，关键参考——ADR 生命周期与 Superseded 双向链接（Nygard 模板/MADR）、记忆写入管线与五操作（promptessor.com 等 2026 记忆架构文章）、CLAUDE.md 分层与长度红线（≤250 行根文件/500 行拆分共识）。

## 6. 本仓库结构

```
touchstone/
├── DESIGN.md      # 设计方案 v1.0（定稿，含评审记录）
├── HANDOVER.md    # 本文件
└── TOUCHSTONE-TEMPLATE.md  # 条目模板与文档骨架示例
```
