# touchstone — 项目结论生命周期管理机制设计方案

> **touchstone**（试金石）：以 PCR（Project Conclusion Records）格式记录、按五态生命周期演进、仅凭用户验证归档的项目事实基准。

> 版本：v1.0（已含多维矩阵评审修订项 P0/P1/P2）
> 日期：2026-09-11
> 状态：设计定稿，待最终优化与试点落地
> Provenance: distilled from a real-world design session on a monitoring-platform AI assistant; internal details generalized.
> 起源：评审佐证对标 ADR（Nygard/MADR）、LLM 记忆生命周期最佳实践、CLAUDE.md/AGENTS.md 分层共识（评审资料见 HANDOVER.md）

---

## 1. 问题定义

现状：AI 编码助手被告知"做事后汲取经验/结论"，但记忆是**平的**——没有置信度分级、没有生命周期。导致两个真实缺陷：

1. **过时结论不死**：环境/项目演进后，旧结论仍被当事实引用，AI 助手做无用功。实证案例（2026-09-11）：9/10 记录的「某平台只转发自有会话 cookie」假设，9/11 被线上实测推翻；若无淘汰机制，后续会话仍会引用。
2. **置信度无差别**：对话中一次推理的"结论"与端到端验收的"结论"被同等对待，后者才是可信事实底座。

目标：让 AI 助手每次开工带着「已验证的最新事实 + 完整的被推翻史」工作，而不是一堆置信度不明的记忆碎片。

## 2. 核心设计

### 2.1 载体：TOUCHSTONE.md（进仓库，append-only 时间线）

- **位置**：各项目仓库根目录（或 `docs/TOUCHSTONE.md`），**随代码走**、git 可追溯、团队可见。原则：结论与产生结论的代码在同一版本控制系统里，杜绝"记忆在、代码不在"的漂移。
- **写入模式**：条目不可变（immutable）——已写入的条目不修改正文；变更通过**新增条目 + 双向链接**实现。
- **粒度**：每仓库一份；跨仓库事项在主仓库文档中只写一行指针。

### 2.2 条目模板（四字段，宁少勿多）

```markdown
## 2026-09-11 · 身份链终型：cookie 通道下线 【已归档】
- 结论：唯一身份链 = X-App-Key + user_name，cookie 身份整体删除
- 证据：修复提交 · CI 构建号 · 生产环境三项实测
- 推翻：2026-09-10「某平台只转发自有会话 cookie」→ 该条目标记【已推翻】
- 影响面：相关 MCP 服务及后续接入方
- 批准人：用户（2026-09-11 会话）
- 复审：+3 个月（依赖外部平台转发行为，外部环境敏感）
```

字段说明：结论一句话、证据指针（commit/构建号/测试结果）、状态标记、关系链接（推翻/被推翻/取代/被取代）、影响面、批准人溯源、复审期限（仅外部环境敏感条目强制）。

### 2.3 状态机（五态，锁死不蔓延）

| 状态 | 进入条件 | AI 助手使用规则 |
|---|---|---|
| **BETA 假设中** Assumption | 对话推理得出，未验证 | 仅作线索，禁止作为实施依据 |
| **RC 验证中** In verification | 正在测试/部署/待验收 | 引用须带"截至何时" |
| **GA 已归档** Archived | 端到端验证通过 **且用户明确说"归档"** | 可作事实直接引用 |
| **SUPERSEDED 已推翻** Refuted | 被新证据否定（有明确替代条目） | 禁止引用，保留原文+双向链接 |
| **EOL 已废弃** Deprecated | 失效但无替代（Deprecated） | 禁止引用，保留原文 |

> 说明：已推翻/已废弃对应 ADR 的 Superseded/Deprecated 二分；保留原文而非删除，因为"为什么错、怎么发现的"是最贵的经验。

### 2.4 两道不可妥协的门禁

1. **归档人工门禁**：AI 助手起草草稿（标"假设中"，挂候选区），**只有用户在对话中明示归档（如"归档这条"）才转为已归档**。AI 助手不能给自己发验收章——自我归档=自我说服，是结论腐化的起点。
2. **推翻/废弃须附证据**：新条目必须给出推翻旧条目的证据指针，禁止"我觉得变了"式软推翻。

## 3. 文档结构（含防膨胀机制）

```markdown
# TOUCHSTONE
## 索引区（≤200 行硬上限）
| 日期 | 主题 | 状态 | 一句话 |
## 已归档（按时间倒序）
## 候选区（假设中/验证中，待用户裁决）
## 已推翻 / 已废弃（保留区，只进不出）
```

防膨胀与保鲜规则（评审修订新增）：

- **P0-1 索引区检索**：AI 助手开工先读索引区（≤200 行），详情按需展开——"检索而非全量注入"。
- **P1-1 强制 TTL**：凡依赖**外部平台行为**的归档条目必须带 `复审：+3个月`；超期条目检索时自动降级为"待复核"，不得直接引用。
- **P1-2 语义去重**：新增条目前查索引区相似主题，命中则 UPDATE 既有条目（新增版本+双向链接）而非另起新条目。对应记忆策略五操作 READ/WRITE/UPDATE/FORGET/VERIFY 的全覆盖。
- **P2-1 长度红线**：单文件 >500 行按年份或主题域拆分；索引区超 200 行触发强制裁剪（老条目折叠为索引行）。
- **主题折叠**：同一主题多轮反复（如一次故障排查四五轮），归档只留终态一条 + 两行演进摘要，中间过程收进证据指针。

## 4. 与现有记忆体系的分工

| 载体 | 管什么 | 性质 |
|---|---|---|
| **TOUCHSTONE.md** | 项目事实与演变（客观、已验证） | 进仓库，团队共享 |
| **MEMORY.md / auto-memory** | 用户偏好、协作教训（如"鉴权决策须与用户确认"） | 私有，跨项目 |
| **对话中的临时结论** | 默认留在对话里 | AI 助手认为值得保留时起草为"假设中"进候选区 |

## 5. 落地路径（终态：三层已落地，2026-09-11）

> 原计划两阶段（两周记忆试点→再 skill 化），用户批准 A 档直接三层落地（防线经三路 subagent 审查后齐备）。原计划存档于 git 历史。

1. **层① Skill**：`~/.claude/skills/touchstone/SKILL.md`（Claude Code 系）——description 枚举用户口语（归档/落账/上线了/搞定了/过时了/推翻…），正文 <400 词：五态表+红线+唯一写入路径+维护上限+退役判据+自我说服对照表
2. **层② SessionStart hook**（用户级 settings.json）：node 脚本（`session-start.js`）——fail-open、git root 唯一定位、注入索引前 30 行+候选区计数、非 UTF-8 静默跳过。**关键实证：本机 bash 无 jq，jq 方案压测 exit 127 后改 node**
3. **层③ 记忆瘦身**：`project-log-lifecycle-rule` 记忆瘦身为指针（规则唯一真源=skill），仅保留"归档/推翻裁决权在用户"边界偏好
4. **未来（可选）**：打包为 marketplace 插件（directory 型 source 即可，无需额外基础设施）

## 6. 评审记录（多维矩阵，2026-09-11）

| 维度 | 评级 | 要点 |
|---|---|---|
| 正确性/完备性 | ✅（修订后） | 五态补齐 ADR 的 Superseded/Deprecated 二分（原四态缺口已修） |
| 架构契合度 | ✅ | 进仓库随代码走，与主流记忆文件实践一致 |
| 可执行性/成本 | ⚠️→✅（修订后） | 索引区检索 + 长度红线 + 主题折叠三重防膨胀（原方案仅一条，已修） |
| 安全/治理 | ✅ | 人工归档门禁 + 批准人溯源 + 语义去重（去重为修订新增） |
| 可演进性 | ✅ | 两阶段落地路径，先规则后 skill |

对标结论：状态机≈ADR 生命周期；写入门禁≈记忆写入管线人工审批实践；载体≈CLAUDE.md/AGENTS.md 分层共识。三个核心直觉均与主流对齐，修订版增加约 15% 规则复杂度，换取 6 个月尺度可持续性。
